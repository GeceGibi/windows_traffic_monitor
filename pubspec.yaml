name: windows_traffic_monitor
description: Live Windows network adapter and connection monitor (no packet payloads).
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.10.0 <4.0.0'

dependencies:
  ffi: ^2.1.3

dev_dependencies:
  lints: ^6.0.0
